#include <stdio.h>

int main( )
{
   printf( "Size of an integer is %i\n", sizeof( int ) );
   printf( "Size of an float is %i\n", sizeof( float ) );
   printf( "Size of an character is %i\n", sizeof( char ) );
   printf( "Size of an long double is %i\n", sizeof( long double ) );

   return 0;
}